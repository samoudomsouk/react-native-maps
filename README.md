# Template for React Native Maps

This is a template for setting up `react-native-maps`.

# Installation

```bash
git clone git@github.com:samoudomsouk/react-native-maps.git
cd react-native-maps
npm install
```
